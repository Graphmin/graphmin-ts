/**
 * @author: Patrik Forsberg <patrik.forsberg@coldmind.com>
 * @date: 2023-01-12 14:43
 */

/*
export class WsComponent {
	interface Target {
	app: Application | Router;
	middleware: Middleware[];
}

	const registerMiddleware = (target: WsComponent, middleware: Middleware) => {
		target.app.use(middleware);
		target.middleware.push(middleware);
	};
}
*/
